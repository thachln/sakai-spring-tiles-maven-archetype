#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0"?>
<registration>
	<tool id="sakai.${parentArtifactId}" 
		title="${parentArtifactId}" 
		description="This description is set in the ${artifactId} registration file, found in the webapp/tools directory">

		<category name="course" />
      	<category name="project" />

		<configuration name="functions.require" /> 
		
    </tool>
</registration>

